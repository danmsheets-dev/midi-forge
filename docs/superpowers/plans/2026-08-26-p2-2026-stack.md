# P2 2026 stack (MIDI-OX never had this)

**Goal:** Live “now” view, named CCs/NRPN, MIDI-CI discovery, Windows MIDI Services diagnostics. Native `MidiSession` still needs the App SDK projection (not on crates.io).

| Item | Where |
|------|--------|
| CC names + RPN/NRPN | `cc.rs`, `nrpn.rs`, decode summary |
| Live channel view | `live.rs` + app panel |
| MIDI-CI discovery | `midi_ci.rs` + SysEx button |
| WMS status | `wms.rs` probe midi.exe / MidiSrv |

- [x] Named CCs in monitor
- [x] NRPN/RPN assembler
- [x] Live view
- [x] MIDI-CI discovery inquiry/reply
- [x] WMS diagnostics (no fake Session)
